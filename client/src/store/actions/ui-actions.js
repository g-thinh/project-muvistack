export const toggleMenu = () => ({
  type: "TOGGLE_MENU",
});

export const toggleMatchModal = (bool) => ({
  type: "TOGGLE_MATCHED_MODAL",
  bool,
});

export const toggleDateModal = (bool) => ({
  type: "TOGGLE_DATE_MODAL",
  bool,
});
